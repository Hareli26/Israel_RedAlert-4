# 🔴 Red Alert Monitor v4.1 — התרעות צבע אדום

> **נבנה על ידי הראלי דודאי — מרץ 2026**

מערכת ניטור התרעות פיקוד העורף בזמן אמת לשולחן העבודה של Windows.
שואב מידע מ-API הרשמי של פיקוד העורף (OREF) ומציג התרעות בממשק גרפי מלא.

---

## 🆕 שינויים בגרסה 4.1

| # | תיאור |
|---|--------|
| ✅ | **התקנה לנתיב קבוע `C:\RedAlertIDF`** — Task Scheduler תמיד מצביע לשם, לא לתיקיית ההורדות |
| ✅ | **`install.bat` חדש** — התקנה מלאה בלחיצה אחת + Scheduled Task + Registry |
| ✅ | **זיהוי מיקום מיידי בהפעלה** — האפליקציה מזהה היכן אתה נמצא עכשיו ומוסיפה את הישוב אוטומטית |
| ✅ | **ישובים קבועים נשמרים תמיד** — המיקום הנוכחי מוסף כישוב *זמני*, הישובים הקבועים שלך תמיד פעילים |
| ✅ | **תצוגה משופרת** — לא עוד חיתוך טקסט בווידג'ט הצף ובהיסטוריית ההתרעות |
| ✅ | **~60 ישובים ומושבים נוספים** — כולל ברקת, גבעת זאב, קרני שומרון, שגב-שלום, ועוד |

---

## ✨ תכונות עיקריות

### 🚨 התרעות בזמן אמת
- שאילתה לשרת פיקוד העורף כל **2 שניות**
- **סינון לפי ישוב** — קבלת התרעות רק לאזור שלך
- סינון אוטומטי של התרעות בדיקה (`cat=101`)
- תמיכה בסוגי אירועים: טיל / כלי טיס / אירוע חירום

---

### 🖥️ רכיבי ממשק

| רכיב | תיאור |
|------|--------|
| **FloatingWidget** | ווידג'ט צף בצד המסך — תמיד גלוי, ניתן לגרור |
| **FullScreen Banner** | מסך מלא עם שם הישוב, פרטי האירוע, שעת ההתרעה ושעון חי |
| **AlertOverlay** | חלון מרחף קומפקטי עם ספירה לאחור וכפתור "פתח מסך מלא" |
| **MapWindow** | מפת Leaflet מוטמעת — מסמנת ישובים תחת התרעה |
| **ShelterMiniBanner** | רצועה צפה אדומה — עוברת לירוק בשחרור פיקוד העורף |
| **FriendAlertBanner** | באנר כתום-זהוב כשאדם קרוב נמצא באזור התרעה |
| **System Tray** | אייקון במגש המערכת עם תפריט מהיר |

---

### 🗺️ מפה
- נפתחת אוטומטית בקבלת התרעה
- **נסגרת אוטומטית לאחר 10 שניות**
- מציגה 3 סוגי סמנים:
  - 🏠 ישובים שלך (כחול)
  - ⚠️ ישובים תחת התרעה (אדום)
  - 👤 אנשים ששיתפו מיקום (ירוק / אדום בהתרעה)

---

### 🏠 זרימת מרחב מוגן

```
קבלת התרעה
     ↓
FullScreen — כפתורי "הבנתי — סגור" / "אני במרחב המוגן"
     ↓ (לחיצה על "במרחב מוגן")
ShelterMiniBanner אדום — "יש להשאר במרחב המוגן"
     ↓ (פיקוד העורף משחרר)
ShelterMiniBanner ירוק — "ניתן לצאת — פיקוד העורף שחרר"
     ↓ (8 שניות)
נסגר אוטומטית
```

---

### 🌍 שיתוף מיקום Google

- **חיבור אוטומטי** עם שמירת קוקיות מסשן לסשן
- כשמיקומי חברים נטענים לראשונה — **דיאלוג מוצג להוספת ישוביהם** לרשימת ההתרעות
- כשחבר/ה נמצאים בישוב תחת התרעה:
  - 🟠 **FriendAlertBanner** כתום מוצג עם השמות והישובים
  - 🔔 **צליל שונה** (ניתן לבחירה בהגדרות)
  - 🗺️ סמן ירוק/אדום על המפה

---

### 📍 זיהוי מיקום אוטומטי (v4.1)

**בכל הפעלה** האפליקציה בודקת היכן אתה נמצא לפי כתובת IP:

```
הפעלת האפליקציה
        ↓
  זיהוי מיקום לפי IP (~2 שניות)
        ↓
  ┌─────────────────────────────────────────┐
  │  אין ישובים שמורים?                    │
  │  → דיאלוג: "זוהה מיקומך — ברקת"       │
  │    לחץ ✓ להוספה קבועה                  │
  ├─────────────────────────────────────────┤
  │  יש ישובים שמורים + אתה בישוב אחר?    │
  │  → הודעת Tray: "נמצאת ב-ברקת —        │
  │    נוסף זמנית לרשימה"                  │
  │    הישובים הקבועים שלך פעילים תמיד!   │
  ├─────────────────────────────────────────┤
  │  כבר בישוב קבוע שלך?                   │
  │  → ✅ הכל תקין, אין פעולה נדרשת        │
  └─────────────────────────────────────────┘
        ↓
  כל 5 דקות: בדיקת שינוי מיקום
  (אם עברת מקום → הודעה ועדכון אוטומטי)
```

> **דוגמה:** נסעת למושב ברקת ופתחת את המחשב →
> תוך שניות תופיע הודעה: *"📍 מיקום זוהה: ברקת — נוסף זמנית לרשימת ההתרעות. הישובים הקבועים שלך (שוהם...) פעילים תמיד."*

---

### 🔊 סאונד
- פרופילי צליל לבחירה: `סטנדרטי` / `דחוף` / `אזעקה` / `עדין`
- **צליל נפרד לחברים בהתרעה** (ניתן לכוון בהגדרות):

| אפשרות | תיאור |
|--------|--------|
| 🔔 ייחודי לחברים | צפצוף מזהה שונה |
| 🔊 כמו התרעה רגילה | אותו צליל ראשי |
| 🎵 עדין | טון נמוך ושקט |
| 🔇 שקט | ללא צליל |
| 📯 / 🚨 | פרופיל ספציפי לבחירה |

---

## 💻 דרישות מערכת

| דרישה | גרסה מינימלית |
|-------|--------------|
| Windows | 10 / 11 |
| Python | 3.9+ |
| PyQt5 | 5.15.0+ |
| PyQtWebEngine | 5.15.0+ |
| requests | 2.28.0+ |

> **הערה:** האפליקציה מבקשת הרשאות Administrator אוטומטית בהפעלה (נדרש עבור Chromium).

---

## 🚀 הפעלה

### ⭐ אפשרות א׳ — התקנה מלאה (מומלץ)
```
לחץ ימני על:  install.bat  ← "הפעל כמנהל מערכת"
```
מתקין ל-`C:\RedAlertIDF`, יוצר Task Scheduler ו-Registry autostart.

### אפשרות ב׳ — הפעלה מהירה (ללא התקנה)
```
לחץ פעמיים על:  הפעל.bat
```
מפעיל ישירות. הגדרות הפעלה-עם-Windows עובדות גם כך.

### אפשרות ג׳ — Python ישיר
```bash
pip install -r requirements.txt
python red_alert.py
```

### אפשרות ד׳ — EXE (לאחר Build)
```
הרץ:  build_exe.bat
אחר-כך פתח:  C:\RedAlertIDF\RedAlertMonitor.exe
```

---

## 🔄 הפעלה אוטומטית עם Windows

### שיטה מומלצת — `install.bat`
```
לחץ ימני על install.bat ← "הפעל כמנהל מערכת"
```
מגדיר הכל אוטומטית ובשאלה בסוף: "הפעל עכשיו? (Y/N)"

### שיטה חלופית — מתוך ההגדרות
1. פתח **⚙ הגדרות** מהווידג'ט הצף
2. סמן ✅ **"🚀 הפעל עם Windows (כמנהל)"**
3. לחץ **שמור**

**מה קורה מאחורי הקלעים:**
```
set_autostart(enable=True)
    ↓
1. מעתיק EXE → C:\RedAlertIDF\RedAlertMonitor.exe  (נתיב קבוע)
    ↓
2. Task Scheduler ← C:\RedAlertIDF\RedAlertMonitor.exe
   (RunLevel=HighestAvailable, Delay=5s אחרי כניסה)
    ↓
3. Registry HKCU\...\Run ← C:\RedAlertIDF\...  (גיבוי)
```

> **למה `C:\RedAlertIDF`?**
> נתיב קבוע שלא תלוי במיקום תיקיית ההורדות.
> גם אחרי העברת הקבצים — Task Scheduler ימצא תמיד את ה-EXE.

---

## 🔨 בניית EXE

```bash
build_exe.bat
```

הקובץ `RedAlertMonitor.exe` ייוצר בתיקיית הפרויקט.

| שלב | פעולה |
|-----|--------|
| 1 | התקנת PyInstaller ותלויות |
| 2 | בנייה כ-EXE יחיד (`--onefile --windowed`) |
| 3 | העתקה לתיקיית הפרויקט |
| 4 | ניקוי קבצי build זמניים |

---

## 📂 קבצים בפרויקט

```
RedAlertMonitor_v4/
├── red_alert.py          ← קוד מקור ראשי (קובץ יחיד)
├── requirements.txt      ← תלויות Python
├── install.bat           ← ✅ התקנה מלאה ל-C:\RedAlertIDF (מומלץ)
├── build_exe.bat         ← בניית EXE עם PyInstaller + העתקה ל-C:\RedAlertIDF
├── הפעל.bat             ← הפעלה מהירה (ללא התקנה)
├── launch.vbs            ← הפעלה ללא חלון console (גם מ-C:\RedAlertIDF)
└── README.md             ← קובץ זה
```

### 📁 תיקיית ההתקנה הקבועה

```
C:\RedAlertIDF\
├── RedAlertMonitor.exe   ← קובץ הרצה (נעתק אוטומטית)
├── red_alert.py          ← קוד מקור (גיבוי)
└── launch.vbs            ← מפעיל חכם
```

> Task Scheduler **תמיד** מצביע ל-`C:\RedAlertIDF\RedAlertMonitor.exe` —
> נתיב קבוע שלא משתנה גם אם הזזת את תיקיית המקור.

---

## ⚙️ הגדרות

פתח **⚙ הגדרות** מהווידג'ט הצף או מתפריט ה-Tray.

| הגדרה | תיאור |
|-------|--------|
| **ישובים** | רשימת ישובים לסינון (ריק = כל ישראל) |
| **סאונד** | הפעל/כבה צלילי התרעה |
| **צליל ראשי** | בחר פרופיל: סטנדרטי / דחוף / אזעקה / עדין |
| **צליל חברים** | סוג צליל כשחבר באזור התרעה |
| **מסך מלא** | פתיחה אוטומטית של FullScreen בהתרעה |
| **מפה** | פתיחה אוטומטית של מפה בהתרעה |
| **Google** | שיתוף מיקום עם Google Maps |
| **זמן מסך מלא** | סגירה אוטומטית אחרי N שניות |
| **זמן חלון מרחף** | הצגת overlay למשך N שניות |
| **הפעל עם Windows** | Scheduled Task + Registry autostart |

### מיקום קובץ הקונפיגורציה
```
%APPDATA%\RedAlert\config.json
```

---

## 🔌 API

```
GET https://www.oref.org.il/WarningMessages/alert/alerts.json
```

| תגובה | משמעות |
|-------|---------|
| גוף ריק (5 bytes BOM) | אין התרעה פעילה |
| JSON עם `id`, `cat`, `title`, `data` | התרעה פעילה |
| `"cat": "101"` | התרעת בדיקה — מסוננת אוטומטית |

---

## 🛠️ פתרון בעיות

### האפליקציה קורסת ללא הודעה
קריסת C++ של Chromium כשרצים כ-Administrator עם GPU מופעל.
**פתרון:** מטופל אוטומטית — הדגלים הבאים מוגדרים לפני `QApplication`:
```
QTWEBENGINE_CHROMIUM_FLAGS = --disable-gpu --disable-software-rasterizer --no-sandbox
QTWEBENGINE_DISABLE_SANDBOX = 1
```

### מקבל התרעות מכל הארץ
ללא ישוב מוגדר — מתקבלות **כל** ההתרעות בישראל.
**פתרון:** פתח **הגדרות** ← לחץ **📍 בחר ישובים** ← הוסף את הישוב שלך.

### זיהוי מיקום לא עובד
- בדוק חיבור לאינטרנט — זיהוי מיקום דורש חיבור פעיל
- אם הישוב שזוהה שגוי — הוסף את הישוב הנכון ידנית בהגדרות
- הישוב שנוסף זמנית יימחק בפעם הבאה שתיסע למקום אחר

### האפליקציה לא עולה עם Windows
**בדיקה מהירה:**
1. פתח Task Scheduler (`taskschd.msc`)
2. חפש "RedAlertMonitor" תחת Task Scheduler Library
3. בדוק שה-Actions מצביע ל: `C:\RedAlertIDF\RedAlertMonitor.exe`

**אם הנתיב שגוי או ישן:**
- הרץ `install.bat` כמנהל — ידרוס ויתקן הכל
- **או:** פתח **הגדרות** ← בטל סימון "הפעל עם Windows" ← שמור ← סמן שוב ← שמור

**בדיקת קיום ה-EXE:**
```
dir C:\RedAlertIDF\RedAlertMonitor.exe
```
אם לא קיים — הרץ `build_exe.bat` ואחר-כך `install.bat`

### לא מגיע סאונד
- בדוק שהסאונד לא מוגדר כ-`שקט` בהגדרות
- בדוק שהרמקולים/אוזניות מחוברים ומופעלים

### שיתוף מיקום Google לא עובד
- לחץ **🌍 התחבר לגוגל** בהגדרות ← התחבר בדפדפן המוטמע
- אם פג תוקף: לחץ **🔓 התנתק** ← התחבר מחדש

### קבצי לוג לאיתור שגיאות
```
%APPDATA%\RedAlert\crash.log        ← שגיאות Python
%APPDATA%\RedAlert\crash.log.flt    ← קריסות C++ (faulthandler)
```

---

## 🏗️ ארכיטקטורה

```
RedAlertApp  (QApplication)
├── AlertWorker              (QThread)   ← polling OREF API כל 2 שניות
├── LocationSharingWorker    (QThread)   ← עדכון מיקומי חברים כל 30 שניות
├── LocationTrackWorker      (QThread)   ← מעקב מיקום IP כל 5 דקות
├── FloatingWidget                       ← ווידג'ט צף ראשי
├── MapWindow                            ← מפת Leaflet (QWebEngineView)
├── FullScreen                           ← מסך התרעה מלא
├── AlertOverlay                         ← חלון מרחף עם ספירה לאחור
├── ShelterMiniBanner                    ← רצועה "במרחב מוגן" / "ניתן לצאת"
├── FriendAlertBanner                    ← באנר כתום לחברים בהתרעה
├── GoogleBrowserWindow                  ← אינטגרציה Google (QWebEngineView)
├── SettingsDialog                       ← חלון הגדרות
├── LocationDetectDialog                 ← זיהוי מיקום (הפעלה ראשונה)
├── TempLocationDialog                   ← הוספת ישוב זמני בנסיעה
└── FriendLocationsDialog                ← הצעת ישובי חברים לניטור
```

---

## 📄 רישיון

פרויקט פרטי — לשימוש אישי.
המידע מגיע מ-API פיקוד העורף הרשמי: [www.oref.org.il](https://www.oref.org.il)

---

<div align="center">

**נבנה על ידי הראלי דודאי — מרץ 2026**
*Python + PyQt5 🇮🇱*

</div>
